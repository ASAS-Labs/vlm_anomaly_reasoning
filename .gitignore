# uv / Python
.venv/
__pycache__/
*.pyc

# Generated artifacts
data/datasets/

# Eval run logs: keep the result artifacts (results.jsonl, report.json, metrics.json,
# run_meta_*.json) since they back the paper's numbers; drop only the vLLM server log,
# which is hundreds of MB and reproducible.
logs/**/vllm_server.log

.claude/settings.local.json
.env
todo.md
tmp/
.venv-pilot/
.venv-sft/

# taxonomy validation app runtime files
data/taxonomy_validation/decisions.sqlite*
data/taxonomy_validation/.secret

# extension video validation app runtime files
data/extension_validation/decisions.sqlite*
data/extension_validation/.secret
